/**
 * config.js
 * The one file you need to edit to turn on the AI Assistant.
 *
 * After deploying the Cloudflare Worker (see README.md -> "AI Assistant
 * Setup"), paste its URL below. It looks like:
 *   https://your-worker-name.your-subdomain.workers.dev
 *
 * Leave it as null to keep the AI Assistant turned off (the floating
 * button won't appear).
 */

const AI_ASSISTANT_ENDPOINT = 'https://bible-app-assistant.joelgomashie42.workers.dev/';
